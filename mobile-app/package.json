{
  "name": "simteks-erp-mobile",
  "version": "1.0.14",
  "private": true,
  "description": "Simteks Tekstil ERP — Android + iOS (Capacitor, ayrı platformlar)",
  "scripts": {
    "build:www": "node ../scripts/build-mobil-app.js",
    "sync:android": "npm run build:www && npx cap sync android",
    "sync:ios": "npm run build:www && npx cap sync ios",
    "sync:all": "npm run build:www && npx cap sync",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios",
    "add:android": "npx cap add android",
    "add:ios": "npx cap add ios"
  },
  "dependencies": {
    "@capacitor/android": "^7.6.8",
    "@capacitor/app": "^7.1.0",
    "@capacitor/core": "^7.6.8",
    "@capacitor/ios": "^7.6.8",
    "@capacitor/splash-screen": "^7.0.3",
    "@capacitor/status-bar": "^7.0.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.6.8"
  }
}
